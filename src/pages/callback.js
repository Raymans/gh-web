import React from 'react';
import { navigate } from 'gatsby';


export default () => {
  //handleAuthentication((state) => navigate(state));

  return (
    <div>
      Logging you in...
    </div>
  );
};
